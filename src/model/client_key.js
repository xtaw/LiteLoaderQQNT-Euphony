class ClientKey {

    constructor(key, keyIndex, expireTime, url) {
        this.key = key;
        this.keyIndex = keyIndex;
        this.expireTime = expireTime;
        this.url = url;
    }

}

export default ClientKey